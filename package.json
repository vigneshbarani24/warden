{
  "name": "warden",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Governance PDP + tamper-evident audit ledger for enterprise AI agents, on Amazon Aurora DSQL.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:migrate": "tsx scripts/migrate.ts",
    "db:verify": "tsx scripts/verify-chain.ts",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
